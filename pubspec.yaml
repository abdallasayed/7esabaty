name: smart_manager_pro
description: The Ultimate Wallet App
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # التخزين والتنسيق
  shared_preferences: ^2.2.0
  intl: ^0.18.1
  # التصميم والخطوط
  google_fonts: ^6.1.0
  fl_chart: ^0.66.0
  cupertino_icons: ^1.0.2
  # الأدوات المتقدمة (الكاميرا، البصمة، الملفات)
  image_picker: ^1.0.7
  local_auth: ^2.1.8
  path_provider: ^2.1.2
  share_plus: ^7.2.1
  csv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
