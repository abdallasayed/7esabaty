name: smart_wallet
description: Professional Wallet App
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0   # لحفظ البيانات
  intl: ^0.18.1               # للتاريخ والعملة
  fl_chart: ^0.66.0           # للرسوم البيانية الذكية (جديد)
  google_fonts: ^6.1.0        # خطوط جميلة (جديد)
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
